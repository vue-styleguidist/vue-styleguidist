{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": ["lib/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}